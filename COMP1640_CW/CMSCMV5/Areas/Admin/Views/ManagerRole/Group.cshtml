@model CMSCMV5.Areas.Admin.Models.CreateGroupModel

<div class="col-lg-6">
    <div class="panel panel-default">
        <div class="panel-heading">
            Details
        </div>
        <div class="panel-body">
            @if (Model != null)
            {
                <div class="row">
                    <div class="col-lg-12">
                        <form role="form" action="@Url.Action("Group")" method="post">
                            @if (TempData["mess"] != null)
                            {
                                <div class="alert alert-success">
                                    @TempData["mess"]
                                </div>
                            }
                            <div class="form-group">
                                <label>Group name</label>
                                @Html.TextBoxFor(x => x.name, new { @class = "form-control" })
                                <p class="help-block">@Html.ValidationMessageFor(x => x.name)</p>
                            </div>
                            <div class="form-group">
                                <label>Describe the function of the group</label>
                                @Html.TextBoxFor(x => x.description, new { @class = "form-control" })
                                <p class="help-block">@Html.ValidationMessageFor(x => x.description)</p>
                            </div>
                            <div class="form-group">
                                <label>Role used</label>
                                @for (int i = 0; i < Model.roles.Count; i++)
                                {
                                    <div class="checkbox">
                                        <label>
                                            @Html.CheckBoxFor(x => Model.roles[i].check)@Model.roles[i].description
                                            @Html.HiddenFor(x => Model.roles[i].name)
                                            @Html.HiddenFor(x => Model.roles[i].description)
                                        </label>
                                    </div>
                                }
                            </div>
                            @Html.HiddenFor(x => x.id)
                            <button type="submit" onclick="return confirm('Are you sure you want to change this rights group data!');" class=" btn btn-success">Save data</button>
                            @if (Model.id != 0)
                            {
                                <a class="btn btn-danger" href="@Url.Action("DeleteGroup", "ManagerRole", new { id = Model.id })" onclick="return confirm('Are you sure you want to delete this rights group!');" style="float:right;">Delete Data</a>
                            }
                        </form>
                    </div>
                </div>
            }
            <!-- /.row (nested) -->
        </div>
        <!-- /.panel-body -->
    </div>
    <!-- /.panel -->
</div>